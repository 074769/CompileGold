<controls:MetroWindow x:Class="CompilePalX.ParameterAdder"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        Title="Parameter Chooser" Height="480" Width="918"  BorderBrush="{DynamicResource MahApps.Brushes.Accent}" BorderThickness="1" >
    <controls:MetroWindow.WindowButtonCommands>
        <controls:WindowButtonCommands LightCloseButtonStyle="{StaticResource LightButtonStyle}" LightMinButtonStyle="{StaticResource LightButtonStyle}" LightMaxButtonStyle="{StaticResource LightButtonStyle}"/>
    </controls:MetroWindow.WindowButtonCommands>
    <Grid Margin="10,10,10,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,8">
            <TextBlock Text="Show:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <ComboBox Name="DebugFilterCombo" Width="220" HorizontalAlignment="Left" SelectionChanged="DebugFilterCombo_SelectionChanged">
                <ComboBoxItem Content="Hide Debugging Options"/>
                <ComboBoxItem Content="Show All Options"/>
            </ComboBox>
        </DockPanel>

        <DataGrid Grid.Row="1" Name="ConfigDataGrid" AutoGenerateColumns="False" CanUserAddRows="False" CanUserSortColumns="True" ItemsSource="{Binding}" >
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="Enabled" Binding="{Binding IsChecked, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" ElementStyle="{DynamicResource MetroDataGridCheckBox}" EditingElementStyle="{DynamicResource MetroDataGridCheckBox}" SortMemberPath="IsChecked"/>
                <DataGridTextColumn Header="Name" Binding="{ Binding Name}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Param" Binding="{ Binding Parameter}" IsReadOnly="True" CanUserSort="False"/>
                <DataGridTextColumn Header="Description" Binding="{ Binding Description}" IsReadOnly="True" CanUserSort="False"/>
                <DataGridTextColumn Header="Issues" Binding="{ Binding Warning}" IsReadOnly="True"/>
            </DataGrid.Columns>
            <DataGrid.GroupStyle>
                <GroupStyle>
                    <GroupStyle.ContainerStyle>
                        <Style TargetType="{x:Type GroupItem}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type GroupItem}">
                                        <Grid>
                                            <Expander IsExpanded="False" x:Name="GroupExpander">
                                                <Expander.Header>
                                                    <DockPanel ToolTip="Parameter may be incompatible with the current game">
                                                        <iconPacks:PackIconMaterial Kind="Alert" Foreground="{DynamicResource CompilePal.Brushes.Warning}" VerticalAlignment="Center" />
                                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Foreground="{DynamicResource MahApps.Brushes.ThemeBackground}" FontSize="14" Margin="5, 2, 5, 0" Visibility="Visible"/>
                                                    </DockPanel>
                                                </Expander.Header>
                                                <Expander.Content>
                                                    <ItemsPresenter/>
                                                </Expander.Content>
                                                <Expander.Style>
                                                    <Style TargetType="Expander" BasedOn="{StaticResource {x:Type Expander}}">
                                                    </Style>
                                                </Expander.Style>
                                            </Expander>
                                            <ItemsPresenter x:Name="Presenter" Visibility="Collapsed"/>
                                        </Grid>
                                        <ControlTemplate.Triggers>
                                            <DataTrigger Binding="{Binding Name}" Value="IsCompatible">
                                                <Setter TargetName="GroupExpander" Property="Visibility" Value="Collapsed"/>
                                                <Setter TargetName="Presenter" Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </GroupStyle.ContainerStyle>
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate>
                            <TextBlock x:Name="PresetHeaderBlock" FontWeight="Bold" FontSize="14" Margin="5, 2, 5, 0" Visibility="Visible"/>
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                </GroupStyle>
            </DataGrid.GroupStyle>
        </DataGrid>

        <Button Grid.Row="2" Click="CloseButton_Click" Margin="0,8,0,0" HorizontalAlignment="Right" Width="100">Close</Button>
    </Grid>
</controls:MetroWindow>
